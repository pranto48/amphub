{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Restricted capability for the main window, limited strictly to remote control boundaries (core and opener defaults; file system and shell operations disabled).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
